/**
 * AuthFormLayout Types
 * File: types.js
 */

const SIZES = {
  SM: 'sm',
  MD: 'md',
  LG: 'lg',
};

const SIZE_KEYS = Object.values(SIZES);

const LAYOUTS = {
  CENTERED: 'centered',
  TWO_COLUMN: 'two-column',
};

const LAYOUT_KEYS = Object.values(LAYOUTS);

export { SIZES, SIZE_KEYS, LAYOUTS, LAYOUT_KEYS };

