/**
 * API Key Permission Detail Route
 * (main)/settings/api-key-permissions/[id]
 * Per app-router.mdc: lightweight page, delegate to platform screen
 */
import { ApiKeyPermissionDetailScreen } from '@platform/screens';

export default function ApiKeyPermissionDetailRoute() {
  return <ApiKeyPermissionDetailScreen />;
}
