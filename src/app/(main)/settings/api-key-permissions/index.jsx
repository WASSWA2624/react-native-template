/**
 * API Key Permissions List Route
 * (main)/settings/api-key-permissions
 * Per app-router.mdc: lightweight page, delegate to platform screen
 */
import { ApiKeyPermissionListScreen } from '@platform/screens';

export default function ApiKeyPermissionsListRoute() {
  return <ApiKeyPermissionListScreen />;
}
