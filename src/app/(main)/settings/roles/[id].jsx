/**
 * Role Detail Route
 * (main)/settings/roles/[id]
 * Per app-router.mdc: lightweight page, delegate to platform screen
 */
import { RoleDetailScreen } from '@platform/screens';

export default function RoleDetailRoute() {
  return <RoleDetailScreen />;
}
