/**
 * Roles List Route
 * (main)/settings/roles
 * Per app-router.mdc: lightweight page, delegate to platform screen
 */
import { RoleListScreen } from '@platform/screens';

export default function RolesListRoute() {
  return <RoleListScreen />;
}
