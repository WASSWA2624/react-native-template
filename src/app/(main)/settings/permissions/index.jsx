/**
 * Permissions List Route
 * (main)/settings/permissions
 * Per app-router.mdc: lightweight page, delegate to platform screen
 */
import { PermissionListScreen } from '@platform/screens';

export default function PermissionsListRoute() {
  return <PermissionListScreen />;
}
