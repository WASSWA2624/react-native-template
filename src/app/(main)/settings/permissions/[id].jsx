/**
 * Permission Detail Route
 * (main)/settings/permissions/[id]
 * Per app-router.mdc: lightweight page, delegate to platform screen
 */
import { PermissionDetailScreen } from '@platform/screens';

export default function PermissionDetailRoute() {
  return <PermissionDetailScreen />;
}
