/**
 * User Profiles List Route
 * (main)/settings/user-profiles
 * Per app-router.mdc: lightweight page, delegate to platform screen
 */
import { UserProfileListScreen } from '@platform/screens';

export default function UserProfilesListRoute() {
  return <UserProfileListScreen />;
}
