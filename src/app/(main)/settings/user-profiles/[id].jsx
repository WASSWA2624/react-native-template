/**
 * User Profile Detail Route
 * (main)/settings/user-profiles/[id]
 * Per app-router.mdc: lightweight page, delegate to platform screen
 */
import { UserProfileDetailScreen } from '@platform/screens';

export default function UserProfileDetailRoute() {
  return <UserProfileDetailScreen />;
}
