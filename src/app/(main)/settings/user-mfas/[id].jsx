/**
 * User MFA Detail Route
 * (main)/settings/user-mfas/[id]
 * Per app-router.mdc: lightweight page, delegate to platform screen
 */
import { UserMfaDetailScreen } from '@platform/screens';

export default function UserMfaDetailRoute() {
  return <UserMfaDetailScreen />;
}
