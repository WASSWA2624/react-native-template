/**
 * User MFAs List Route
 * (main)/settings/user-mfas
 * Per app-router.mdc: lightweight page, delegate to platform screen
 */
import { UserMfaListScreen } from '@platform/screens';

export default function UserMfasListRoute() {
  return <UserMfaListScreen />;
}
