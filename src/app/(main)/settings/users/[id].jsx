/**
 * User Detail Route
 * (main)/settings/users/[id]
 * Per app-router.mdc: lightweight page, delegate to platform screen
 */
import { UserDetailScreen } from '@platform/screens';

export default function UserDetailRoute() {
  return <UserDetailScreen />;
}
