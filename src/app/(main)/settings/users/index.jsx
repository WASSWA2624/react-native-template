/**
 * Users List Route
 * (main)/settings/users
 * Per app-router.mdc: lightweight page, delegate to platform screen
 */
import { UserListScreen } from '@platform/screens';

export default function UsersListRoute() {
  return <UserListScreen />;
}
