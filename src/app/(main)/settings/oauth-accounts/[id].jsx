/**
 * OAuth Account Detail Route
 * (main)/settings/oauth-accounts/[id]
 * Per app-router.mdc: lightweight page, delegate to platform screen
 */
import { OauthAccountDetailScreen } from '@platform/screens';

export default function OauthAccountDetailRoute() {
  return <OauthAccountDetailScreen />;
}
