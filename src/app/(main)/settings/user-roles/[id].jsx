/**
 * User Role Detail Route
 * (main)/settings/user-roles/[id]
 * Per app-router.mdc: lightweight page, delegate to platform screen
 */
import { UserRoleDetailScreen } from '@platform/screens';

export default function UserRoleDetailRoute() {
  return <UserRoleDetailScreen />;
}
