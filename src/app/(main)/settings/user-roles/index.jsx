/**
 * User Roles List Route
 * (main)/settings/user-roles
 * Per app-router.mdc: lightweight page, delegate to platform screen
 */
import { UserRoleListScreen } from '@platform/screens';

export default function UserRolesListRoute() {
  return <UserRoleListScreen />;
}
