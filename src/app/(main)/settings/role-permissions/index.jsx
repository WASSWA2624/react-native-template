/**
 * Role Permissions List Route
 * (main)/settings/role-permissions
 * Per app-router.mdc: lightweight page, delegate to platform screen
 */
import { RolePermissionListScreen } from '@platform/screens';

export default function RolePermissionsListRoute() {
  return <RolePermissionListScreen />;
}
