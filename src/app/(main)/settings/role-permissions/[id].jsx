/**
 * Role Permission Detail Route
 * (main)/settings/role-permissions/[id]
 * Per app-router.mdc: lightweight page, delegate to platform screen
 */
import { RolePermissionDetailScreen } from '@platform/screens';

export default function RolePermissionDetailRoute() {
  return <RolePermissionDetailScreen />;
}
