/**
 * API Keys List Route
 * (main)/settings/api-keys
 * Per app-router.mdc: lightweight page, delegate to platform screen
 */
import { ApiKeyListScreen } from '@platform/screens';

export default function ApiKeysListRoute() {
  return <ApiKeyListScreen />;
}
