/**
 * API Key Detail Route
 * (main)/settings/api-keys/[id]
 * Per app-router.mdc: lightweight page, delegate to platform screen
 */
import { ApiKeyDetailScreen } from '@platform/screens';

export default function ApiKeyDetailRoute() {
  return <ApiKeyDetailScreen />;
}
