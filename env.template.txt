# ============================================
# Biomedical Marketplace Frontend - Environment Variables Template
# ============================================
# Copy this file to .env, then update with your actual values
# Never commit .env to version control
# ============================================
#
# IMPORTANT SECURITY NOTE:
# All environment variables in Expo/React Native are PUBLIC and accessible
# in client-side code. NEVER include secrets, API keys, or sensitive data.
# Only use environment variables for non-sensitive configuration (e.g., base URLs).
# ============================================

# ============================================
# Application Configuration
# ============================================
# Node environment: development, staging, production
# Controls logging, debugging, and development features
NODE_ENV="development"

# ============================================
# API Configuration
# ============================================
# Backend API base URL (REQUIRED)
# Development: http://localhost:3000
# Production: https://api.yourdomain.com
# Note: Must include protocol (http:// or https://) and no trailing slash
EXPO_PUBLIC_API_BASE_URL="http://localhost:3000"

# API version (optional, defaults to "v1")
# Used to construct API endpoints: {API_BASE_URL}/api/{API_VERSION}/...
EXPO_PUBLIC_API_VERSION="v1"

# ============================================
# WebSocket Configuration (optional)
# ============================================
# WebSocket server URL for real-time features
# If not set, will attempt to derive from API_BASE_URL
# Format: ws://localhost:3001 or wss://api.yourdomain.com
# EXPO_PUBLIC_WS_URL="ws://localhost:3001"

# ============================================
# Feature Flags (optional)
# ============================================
# Enable/disable features at runtime
# Values: "true" or "false" (as strings)
# EXPO_PUBLIC_ENABLE_ANALYTICS="true"
# EXPO_PUBLIC_ENABLE_CRASH_REPORTING="false"
# EXPO_PUBLIC_ENABLE_DEBUG_LOGGING="false"

# ============================================
# Development/Testing Configuration (optional)
# ============================================
# Enable React Native debugger
# EXPO_PUBLIC_ENABLE_DEBUGGER="true"

# Mock API responses for testing (development only)
# EXPO_PUBLIC_MOCK_API="false"

# ============================================
# Build Configuration (optional)
# ============================================
# App version override (if different from package.json)
# Defaults to "0.1.0" if not set
EXPO_PUBLIC_APP_VERSION="0.1.0"

# Build number (for mobile apps)
# Defaults to "0" if not set
EXPO_PUBLIC_BUILD_NUMBER="0"

# App environment (optional, defaults to NODE_ENV)
# Used to distinguish between different deployment environments
# Values: development, staging, production
# EXPO_PUBLIC_APP_ENVIRONMENT="development"

# ============================================
# Third-Party Services (optional)
# ============================================
# Analytics service keys (public keys only, never secrets)
# EXPO_PUBLIC_ANALYTICS_KEY="your-public-analytics-key"

# Error tracking service (public DSN only)
# EXPO_PUBLIC_SENTRY_DSN="your-public-sentry-dsn"

# ============================================
# Platform-Specific Configuration (optional)
# ============================================
# iOS Configuration
# EXPO_PUBLIC_IOS_BUNDLE_ID="com.yourcompany.biomed"

# Android Configuration
# EXPO_PUBLIC_ANDROID_PACKAGE="com.yourcompany.biomed"

# ============================================
# Notes
# ============================================
# 1. All variables prefixed with EXPO_PUBLIC_ are accessible in client-side code
# 2. Variables without EXPO_PUBLIC_ prefix are NOT accessible in the app
# 3. Changes to .env require app restart (expo start --clear)
# 4. For production builds, set these in your CI/CD environment
# 5. Never commit .env file to version control

